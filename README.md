# Witech Event Planer
App for event management

# Dependencies
* `yarn`

# Development
*  `yarn dev`

# Production
*  `yarn start:server`

# Branch
`$username` -> (Pull Request) -> `develop` -> (Pull Request) -> `master`